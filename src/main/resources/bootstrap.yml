spring:
  application:
    name: zuul-server
  cloud:
    config:
      uri: http://localhost:8888

#Para poder refrescar desde actuator
management:
  endpoints:
    web:
      exposure:
        include: "*"